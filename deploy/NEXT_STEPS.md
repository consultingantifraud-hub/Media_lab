# 📋 Следующие шаги для завершения интеграции биллинга

## ✅ Что уже сделано

- ✅ Модели БД (users, balances, payments, operations, discount_codes)
- ✅ Сервисы биллинга и платежей
- ✅ Обработчики оплаты и баланса в боте
- ✅ Webhook endpoint для ЮKassa
- ✅ Система промокодов
- ✅ Механизм бесплатного доступа
- ✅ Миграции Alembic
- ✅ Документация

## 🚀 Что нужно сделать

### 1. Применить миграции БД

```bash
cd /opt/media-lab
alembic upgrade head
```

Это создаст все необходимые таблицы:
- users (с полем has_free_access)
- balances
- payments
- operations
- discount_codes
- user_discount_codes

### 2. Создать стандартные промокоды

```bash
python scripts/init_discount_codes.py
```

Создаст промокоды:
- WELCOME10 (10%)
- SAVE20 (20%)
- BONUS30 (30%)
- MEGA50 (50%)
- FREE_ACCESS (бесплатный доступ)

### 3. Обновить .env файл

Добавьте в `.env` на сервере:

```bash
# Billing Configuration
PRICE_PER_OPERATION=10
FREE_OPERATIONS_COUNT=4

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost/media_lab
# или для SQLite: DATABASE_URL=sqlite:///./media_lab.db

# YooKassa Configuration
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
YOOKASSA_RETURN_URL=https://t.me/your_bot
YOOKASSA_CURRENCY=RUB
YOOKASSA_WEBHOOK_URL=https://your-domain.com/yookassa/webhook
```

### 4. Установить зависимости

```bash
pip install sqlalchemy alembic
# или если используете PostgreSQL:
pip install sqlalchemy alembic psycopg2-binary
```

### 5. Подключить роутеры в коде

#### В `app/bot/main.py`:

```python
from app.bot.handlers import billing

# После создания диспетчера
router.include_router(billing.router)
# или
dp.include_router(billing.router)
```

#### В `app/web/api.py`:

```python
from app.web import billing

# После создания app
app.include_router(billing.router)
```

### 6. Добавить декораторы в платные обработчики

Для каждого платного режима добавьте декоратор `@check_balance_decorator`:

#### Пример для генерации (`app/bot/handlers/generate.py` или где у вас обработчик):

```python
from app.bot.handlers.billing import check_balance_decorator

@router.message(...)  # ваш фильтр
@check_balance_decorator("generate")
async def handle_generate(message: Message, operation_id: int):
    # operation_id будет передан автоматически
    # ваш существующий код
    ...
```

#### Режимы, которые нужно обновить:

- 🎨 **Создать** → `@check_balance_decorator("generate")`
- ✏️ **Редактировать** → `@check_balance_decorator("edit")`
- 🔗 **Объединить** → `@check_balance_decorator("merge")`
- 🔄 **Заменить лицо** → `@check_balance_decorator("face_swap")`
- ✨ **Ретушь** → `@check_balance_decorator("retouch")`
- ⬆️ **Улучшить** → `@check_balance_decorator("upscale")`

**Режим "📝 Добавить текст" НЕ требует изменений** (он бесплатный).

### 7. Добавить кнопку "Оплатить" в главное меню

В обработчике `/start` (`app/bot/handlers/start.py`):

```python
from aiogram.types import InlineKeyboardButton, InlineKeyboardMarkup

def build_main_keyboard():
    return InlineKeyboardMarkup(inline_keyboard=[
        # ... существующие кнопки ...
        [
            InlineKeyboardButton(
                text="💰 Оплатить / Баланс",
                callback_data="payment_menu"
            )
        ],
    ])
```

### 8. Обработка ошибок в воркерах

В воркерах (`app/workers/`) при ошибках Fal.ai добавьте возврат средств:

```python
from app.services.billing import BillingService
from app.db.base import SessionLocal

def process_image_task(task_data):
    operation_id = task_data.get("operation_id")
    
    try:
        # ваш код обработки через Fal.ai
        result = fal_client.generate(...)
        return result
    except Exception as e:
        # При ошибке - вернуть средства
        if operation_id:
            db = SessionLocal()
            try:
                # Проверить, была ли операция платной
                from app.db.models import Operation, OperationStatus
                operation = db.query(Operation).filter(Operation.id == operation_id).first()
                
                if operation and operation.status == OperationStatus.CHARGED:
                    # Вернуть средства
                    BillingService.refund_operation(db, operation_id)
                    logger.info(f"Refunded operation due to error: operation_id={operation_id}")
            finally:
                db.close()
        
        raise  # Пробросить ошибку дальше
```

### 9. Настроить webhook в ЮKassa

1. Войдите в личный кабинет ЮKassa
2. Перейдите в "Настройки" → "HTTP-уведомления"
3. Укажите URL: `https://your-domain.com/yookassa/webhook`
4. Сохраните настройки

### 10. Перезапустить сервисы

```bash
sudo systemctl restart media-lab-bot
sudo systemctl restart media-lab-api
```

### 11. Активировать бесплатный доступ для себя

#### Вариант 1: Через промокод
1. Отправьте боту `/balance`
2. Выберите "🎟️ Промокод для генераций"
3. Введите `FREE_ACCESS`

#### Вариант 2: Через БД
```sql
UPDATE users 
SET has_free_access = true 
WHERE telegram_id = YOUR_TELEGRAM_ID;
```

### 12. Протестировать функционал

#### Тест 1: Команда /balance
- Отправьте `/balance` боту
- Должно показать баланс и бесплатные операции (или статус бесплатного доступа)

#### Тест 2: Промокод при оплате
1. Нажмите "💰 Оплатить / Баланс"
2. Выберите "🎟️ Ввести промокод"
3. Введите `WELCOME10`
4. Выберите сумму (например, 100₽)
5. Должна примениться скидка 10%

#### Тест 3: Платная операция
1. Выберите платный режим (например, "Создать")
2. Если есть бесплатные операции → должна использоваться бесплатная
3. Если нет бесплатных → должна списаться сумма с баланса
4. Если нет баланса → должно появиться сообщение с кнопкой оплаты

#### Тест 4: Бесплатный доступ
1. Активируйте FREE_ACCESS промокод
2. Выполните платную операцию
3. Операция должна выполниться бесплатно

## 📚 Документация

- `BILLING_DEPLOYMENT.md` - полная инструкция по развертыванию
- `BILLING_INTEGRATION_GUIDE.md` - руководство по интеграции
- `INTEGRATION_EXAMPLES.md` - примеры кода
- `DISCOUNT_CODES_GUIDE.md` - руководство по промокодам
- `FREE_ACCESS_GUIDE.md` - руководство по бесплатному доступу

## ⚠️ Важные замечания

1. **Резервное копирование БД** перед применением миграций
2. **Тестовый режим ЮKassa** для разработки
3. **Проверка логов** после перезапуска сервисов
4. **Мониторинг** использования промокодов и платежей

## 🔍 Проверка работоспособности

### Логи бота:
```bash
sudo journalctl -u media-lab-bot -f | grep billing
```

### Логи API (webhook):
```bash
sudo journalctl -u media-lab-api -f | grep yookassa
```

### Проверка БД:
```sql
-- Проверить созданные таблицы
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- Проверить промокоды
SELECT code, discount_percent, is_active FROM discount_codes;
```

## ✅ Чеклист готовности

- [ ] Миграции применены (`alembic upgrade head`)
- [ ] Промокоды созданы (`python scripts/init_discount_codes.py`)
- [ ] .env файл обновлен
- [ ] Зависимости установлены
- [ ] Роутеры подключены в main.py и api.py
- [ ] Декораторы добавлены в платные обработчики
- [ ] Кнопка "Оплатить" добавлена в меню
- [ ] Обработка ошибок в воркерах добавлена
- [ ] Webhook настроен в ЮKassa
- [ ] Сервисы перезапущены
- [ ] Бесплатный доступ активирован для администратора
- [ ] Функционал протестирован

После выполнения всех шагов система биллинга будет полностью готова к работе!



