# Пошаговая инструкция развертывания на сервере

## ✅ Шаг 1: Проверка Docker (на сервере)

Выполните на сервере:

```bash
# Проверить, установлен ли Docker
docker --version

# Если Docker не установлен, выполните:
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
rm get-docker.sh

# Проверить Docker Compose
docker-compose --version

# Если не установлен:
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

## 📦 Шаг 2: Загрузка проекта на сервер

### Вариант А: Через SCP (с локальной машины Windows)

Откройте **новое окно PowerShell** на вашем компьютере и выполните:

```powershell
# Перейти в корневую директорию проекта Media_lab
cd C:\MAIN\Bots\Media_lab

# Создать директорию на сервере
ssh reg-ru-neurostudio "mkdir -p /opt/media-lab"

# Загрузить файлы проекта на сервер
scp -r * reg-ru-neurostudio:/opt/media-lab/
```

### Вариант Б: Через Git (если проект в репозитории)

На сервере выполните:

```bash
cd /opt
sudo mkdir -p media-lab
sudo chown $USER:$USER media-lab
cd media-lab
git clone <ваш-репозиторий> .
```

## 🔧 Шаг 3: Настройка сервера (на сервере)

На сервере выполните:

```bash
cd /opt/media-lab/deploy

# Сделать скрипты исполняемыми
chmod +x scripts/*.sh
chmod +x monitoring/*.sh

# Запустить скрипт автоматической настройки
./scripts/setup.sh
```

**Важно:** После выполнения скрипта настройки перезайдите в систему:
```bash
exit
# Затем подключитесь снова
ssh reg-ru-neurostudio
```

## ⚙️ Шаг 4: Настройка конфигурации (на сервере)

```bash
cd /opt/media-lab

# Скопировать пример конфигурации
cp deploy/env.prod.example .env

# Отредактировать .env файл
nano .env
```

Заполните обязательные переменные:
- `tg_bot_token` - токен вашего Telegram бота (получить у @BotFather)
- `fal_api_key` - API ключ fal.ai (получить на https://fal.ai/dashboard/keys)
- `app_env=vps` - режим работы (должен быть "vps")

Для сохранения в nano: `Ctrl+O` (сохранить), `Enter` (подтвердить), `Ctrl+X` (выйти)

## 📁 Шаг 5: Создание директорий (на сервере)

```bash
cd /opt/media-lab

# Создать директории для медиа файлов
mkdir -p media/images media/edits media/face_swap media/videos

# Установить права доступа
chmod -R 755 media
```

## 🚀 Шаг 6: Запуск сервисов (на сервере)

```bash
cd /opt/media-lab/deploy

# Запустить все сервисы
./scripts/start.sh

# Проверить статус
./scripts/status.sh

# Просмотреть логи
./scripts/logs.sh
```

## ✅ Шаг 7: Проверка работы (на сервере)

```bash
# Проверить статус контейнеров
docker ps

# Проверить логи бота
./scripts/logs.sh bot

# Проверить API
curl http://localhost:8000/health
```

## 📝 Быстрые команды для управления

```bash
cd /opt/media-lab/deploy

# Запуск
./scripts/start.sh

# Остановка
./scripts/stop.sh

# Перезапуск
./scripts/restart.sh

# Статус
./scripts/status.sh

# Логи
./scripts/logs.sh [service_name]
```







