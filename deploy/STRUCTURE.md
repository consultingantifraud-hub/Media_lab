# Структура папки deploy

Эта папка содержит все необходимые файлы для развертывания проекта на VPS сервере reg.ru.

## 📁 Структура файлов

```
deploy/
├── README.md                    # Полная документация по развертыванию
├── INSTALL.md                    # Пошаговая инструкция по установке
├── QUICK_START.md               # Быстрый старт
├── DEPLOYMENT_CHECKLIST.md      # Чеклист развертывания
├── STRUCTURE.md                 # Этот файл
├── env.prod.example             # Пример конфигурации для production
├── docker-compose.prod.yml      # Docker Compose конфигурация для production
│
├── scripts/                     # Скрипты управления
│   ├── setup.sh                # Первоначальная настройка VPS
│   ├── start.sh                # Запуск всех сервисов
│   ├── stop.sh                 # Остановка всех сервисов
│   ├── restart.sh              # Перезапуск всех сервисов
│   ├── status.sh               # Проверка статуса
│   ├── logs.sh                 # Просмотр логов
│   ├── update.sh               # Обновление проекта
│   └── backup.sh               # Резервное копирование
│
├── systemd/                     # Systemd service файлы
│   ├── media-lab-bot.service   # Сервис для бота
│   ├── media-lab-worker.service # Сервис для воркера
│   ├── media-lab-api.service   # Сервис для API
│   └── media-lab-redis.service # Сервис для Redis
│
├── nginx/                       # Конфигурация Nginx (опционально)
│   └── media-lab.conf          # Конфигурация для API через домен
│
└── monitoring/                  # Скрипты мониторинга
    ├── healthcheck.sh          # Проверка здоровья сервисов
    └── cleanup.sh              # Очистка старых данных
```

## 🚀 Быстрый старт

1. Прочитайте `INSTALL.md` для пошаговой инструкции
2. Или используйте `QUICK_START.md` для минимальных шагов
3. Используйте `DEPLOYMENT_CHECKLIST.md` для проверки всех шагов

## 📝 Основные файлы

### Конфигурация

- **`env.prod.example`** - Пример файла конфигурации. Скопируйте в `.env` и заполните переменные.
- **`docker-compose.prod.yml`** - Production конфигурация Docker Compose с healthchecks и оптимизациями.

### Скрипты управления

Все скрипты в `scripts/` готовы к использованию после установки прав:
```bash
chmod +x scripts/*.sh
```

### Systemd сервисы

Сервисы в `systemd/` позволяют управлять приложением через systemd для автозапуска и мониторинга.

### Мониторинг

Скрипты в `monitoring/` можно настроить в cron для автоматического мониторинга и очистки.

## 🔧 Использование

### Первоначальная настройка

```bash
cd /opt/media-lab/deploy
./scripts/setup.sh
```

### Запуск

```bash
./scripts/start.sh
```

### Управление

```bash
./scripts/status.sh    # Статус
./scripts/logs.sh     # Логи
./scripts/restart.sh  # Перезапуск
./scripts/stop.sh     # Остановка
```

### Обновление

```bash
./scripts/update.sh
```

### Резервное копирование

```bash
./scripts/backup.sh
```

## 📚 Документация

- **README.md** - Полная документация с детальными инструкциями
- **INSTALL.md** - Пошаговая инструкция по установке
- **QUICK_START.md** - Минимальные шаги для быстрого запуска
- **DEPLOYMENT_CHECKLIST.md** - Чеклист для проверки всех шагов развертывания


