# Резюме реализации системы биллинга

## ✅ Что реализовано

### 1. База данных (SQLAlchemy + Alembic)

**Файлы:**
- `app/db/base.py` - базовая конфигурация БД
- `app/db/models.py` - модели: User, Balance, Payment, Operation
- `app/db/session.py` - утилиты для работы с сессиями
- `alembic/` - конфигурация миграций
- `alembic/versions/001_initial_billing_tables.py` - первая миграция

**Таблицы:**
- `users` - пользователи с полем `free_operations_left`
- `balances` - балансы пользователей в рублях
- `payments` - платежи через ЮKassa
- `operations` - операции биллинга (списания/бесплатные)

### 2. Сервисы

**app/services/billing.py:**
- `BillingService.get_or_create_user()` - создание пользователя с 4 бесплатными операциями
- `BillingService.charge_operation()` - списание средств (сначала бесплатные, потом баланс)
- `BillingService.refund_operation()` - возврат средств при ошибках
- `BillingService.add_balance()` - пополнение баланса
- `BillingService.get_user_info()` - получение информации о балансе

**app/services/payment.py:**
- `PaymentService.create_payment()` - создание платежа в ЮKassa
- `PaymentService.process_webhook()` - обработка webhook от ЮKassa
- Защита от повторной обработки платежей

### 3. Обработчики бота

**app/bot/handlers/billing.py:**
- `/balance` - команда для просмотра баланса
- `payment_menu` - меню оплаты с фиксированными суммами (100/300/500/1000₽)
- `payment_custom` - ввод произвольной суммы
- `check_balance_decorator()` - декоратор для проверки баланса перед платными операциями

### 4. Webhook API

**app/web/billing.py:**
- `POST /yookassa/webhook` - endpoint для получения уведомлений от ЮKassa
- `GET /yookassa/health` - проверка работоспособности биллинга

### 5. Конфигурация

**env.prod.example:**
- Добавлены переменные для биллинга и ЮKassa
- Добавлена переменная `DATABASE_URL`

**requirements-billing.txt:**
- SQLAlchemy >= 2.0.0
- Alembic >= 1.13.0
- httpx >= 0.27.0 (уже в requirements.txt)

### 6. Документация

- `BILLING_DEPLOYMENT.md` - полная инструкция по развертыванию
- `BILLING_INTEGRATION_GUIDE.md` - руководство по интеграции в существующие обработчики
- `INTEGRATION_EXAMPLES.md` - примеры кода
- `BILLING_IMPLEMENTATION_PLAN.md` - план реализации

## 🔄 Что нужно сделать для завершения

### 1. Интеграция в существующие обработчики

**Файлы для изменения:**
- `app/bot/main.py` - добавить `router.include_router(billing.router)`
- `app/web/api.py` - добавить `app.include_router(billing.router)`
- Обработчики платных режимов - добавить декоратор `@check_balance_decorator("operation_type")`

**Платные режимы (требуют интеграции):**
- 🎨 Создать → `@check_balance_decorator("generate")`
- ✏️ Редактировать → `@check_balance_decorator("edit")`
- 🔗 Объединить → `@check_balance_decorator("merge")`
- 🔄 Заменить лицо → `@check_balance_decorator("face_swap")`
- ✨ Ретушь → `@check_balance_decorator("retouch")`
- ⬆️ Улучшить → `@check_balance_decorator("upscale")`

**Бесплатный режим (не требует изменений):**
- 📝 Добавить текст - работает без биллинга

### 2. Добавление кнопки в главное меню

В обработчике `/start` добавить кнопку:
```python
InlineKeyboardButton(text="💰 Оплатить / Баланс", callback_data="payment_menu")
```

### 3. Обработка ошибок в воркерах

В воркерах при ошибках Fal.ai добавить возврат средств:
```python
if operation_id:
    BillingService.refund_operation(db, operation_id)
```

### 4. Развертывание

1. Обновить `.env` с переменными биллинга
2. Установить зависимости: `pip install -r requirements.txt`
3. Применить миграции: `alembic upgrade head`
4. Настроить webhook URL в ЮKassa
5. Перезапустить сервисы

## 📊 Логика работы

### Новый пользователь:
1. При первом `/start` создается пользователь с `free_operations_left = 4`
2. Первые 4 платные операции бесплатные
3. После исчерпания бесплатных операций - списание с баланса

### Платная операция:
1. Проверка бесплатных операций → если есть, уменьшить счетчик
2. Если нет бесплатных → проверка баланса
3. Если баланс >= 10₽ → списать 10₽, выполнить операцию
4. Если баланс < 10₽ → сообщение об ошибке + кнопка оплаты

### Оплата:
1. Пользователь выбирает сумму (100/300/500/1000 или произвольную)
2. Создается платеж в ЮKassa
3. Пользователь переходит по ссылке и оплачивает
4. ЮKassa отправляет webhook
5. Баланс пополняется автоматически

### Возврат средств:
1. При ошибке Fal.ai/инфраструктуры проверяется `operation_id`
2. Если операция была платной (`status = charged`) → возврат 10₽ на баланс
3. Операция помечается как `refunded`

## 🔐 Безопасность

- Транзакции БД для атомарных операций списания/пополнения
- Защита от повторной обработки webhook (проверка статуса платежа)
- Использование `with_for_update()` для блокировок при параллельных запросах
- Валидация сумм платежей (минимум 10₽)

## 📝 Важные замечания

1. **База данных:** Для production рекомендуется PostgreSQL, для разработки можно использовать SQLite
2. **Webhook URL:** Должен быть доступен по HTTPS из интернета
3. **Тестовый режим ЮKassa:** Используйте тестовые ключи для разработки
4. **Миграции:** Всегда делайте backup БД перед применением миграций
5. **Мониторинг:** Следите за логами биллинга и платежей

## 🚀 Быстрый старт

1. Добавить переменные в `.env` (см. `env.prod.example`)
2. Установить зависимости: `pip install sqlalchemy alembic`
3. Применить миграции: `alembic upgrade head`
4. Подключить роутеры в `main.py` и `api.py`
5. Добавить декораторы в платные обработчики
6. Перезапустить сервисы

Подробные инструкции см. в `BILLING_DEPLOYMENT.md` и `BILLING_INTEGRATION_GUIDE.md`.




