# 🔄 Работа с Git и автоматическое обновление на GitHub

## 📋 Текущий статус

✅ **Репозиторий:** `https://github.com/consultingantifraud-hub/Media_lab`  
✅ **SSH подключение:** Настроено и работает  
✅ **Бранч:** `main`  
✅ **Автоматическое обновление:** Настроено (2 раза в день: 07:00 и 16:00)

---

## 🔄 Как обновлять код на GitHub

### Вариант 1: Автоматическое обновление (рекомендуется)

Настроено автоматическое обновление 2 раза в день:
- **07:00** - автоматический коммит и отправка
- **16:00** - автоматический коммит и отправка

Скрипт использует расширенную фильтрацию (Вариант 3):
- ✅ Включает только важные файлы (код, конфигурация, документация)
- ✅ Исключает временные файлы, логи, базы данных, медиа

**Ручной запуск:**
```bash
cd /opt/media-lab
bash auto_commit.sh
```

### Вариант 2: Ручное обновление

```bash
cd /opt/media-lab

# 1. Проверить статус
git status

# 2. Добавить изменения
git add -A

# 3. Создать коммит
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push origin main
```

### Вариант 3: Быстрое обновление (одной командой)

```bash
cd /opt/media-lab && git add -A && git commit -m "Update: $(date '+%Y-%m-%d %H:%M:%S')" && git push origin main
```

---

## 📥 Получение обновлений с GitHub

Если на GitHub есть изменения, которых нет на сервере:

```bash
cd /opt/media-lab
git pull origin main
```

---

## 🔐 Что НЕ попадает в репозиторий

Следующие файлы автоматически игнорируются (см. `.gitignore`):
- ✅ `.env` - файлы с секретами (токены, ключи API)
- ✅ `*.db` - базы данных
- ✅ `media/images/*` - сгенерированные изображения
- ✅ `media/videos/*` - сгенерированные видео
- ✅ `*.log` - логи
- ✅ `__pycache__/` - кэш Python

---

## 🎯 Система фильтрации (Вариант 3: Расширенная)

### ✅ БЕЛЫЙ СПИСОК (что отправляется):

**Код:**
- `*.py` - Python код
- `*.sh` - Скрипты

**Конфигурация:**
- `*.yml`, `*.yaml` - YAML конфигурация
- `*.json` - JSON конфигурация
- `docker-compose.yml` - Docker Compose

**Docker:**
- `Dockerfile*` - Docker файлы
- `.dockerignore` - Docker ignore

**Документация:**
- `*.md` - Markdown
- `*.txt` - Текстовые файлы

**Зависимости:**
- `requirements*.txt` - Python зависимости

**Специальные пути:**
- `app/` - Вся директория приложения
- `docker/` - Docker конфигурация
- `README.md` - Основной README
- `.gitignore` - Git ignore

### ❌ ЧЕРНЫЙ СПИСОК (что НЕ отправляется):

**Временные файлы:**
- `*.log` - Логи
- `*.tmp`, `*.temp` - Временные файлы
- `*.cache` - Кэш
- `*.bak`, `*.backup` - Бэкапы

**Скомпилированные:**
- `*.pyc`, `*.pyo` - Скомпилированный Python
- `__pycache__/` - Python кэш

**Данные:**
- `*.db`, `*.sqlite*` - Базы данных
- `media/images/*` - Изображения
- `media/videos/*` - Видео

**Секреты:**
- `.env` - Переменные окружения

---

## 📊 Проверка синхронизации

Проверить, синхронизирован ли репозиторий:

```bash
cd /opt/media-lab
git fetch origin
git status
```

Если видите `Your branch is up to date with 'origin/main'` - всё синхронизировано.

---

## 🚀 Типичный workflow

1. **Внесение изменений** на сервере
2. **Проверка изменений:** `git status`
3. **Добавление в индекс:** `git add -A`
4. **Создание коммита:** `git commit -m "описание"`
5. **Отправка на GitHub:** `git push origin main`

Или просто дождаться автоматического обновления (07:00 или 16:00).

---

## 🔍 Просмотр истории

```bash
# Последние коммиты
git log --oneline -10

# Детальная информация о коммите
git show <commit_hash>

# Просмотр изменений между коммитами
git diff HEAD~1 HEAD
```

---

## 🔧 Управление автоматическим обновлением

### Просмотр расписания

```bash
crontab -l | grep auto_commit
```

### Просмотр логов

```bash
# Логи автоматических коммитов
tail -f /opt/media-lab/logs/git_auto_commit.log

# Логи cron
tail -f /opt/media-lab/logs/git_cron.log
```

### Изменение частоты обновлений

```bash
cd /opt/media-lab
bash setup_daily_updates.sh
```

### Отключение автоматического обновления

```bash
crontab -e
# Удалить строки с auto_commit.sh
```

---

## 📝 Примеры сообщений коммитов

- `Update: исправление бага в генерации изображений`
- `Feature: добавлена поддержка новой модели`
- `Fix: исправлена проблема с базой данных`
- `Refactor: оптимизация кода воркера`

---

## ⚠️ Важные замечания

1. **Всегда проверяйте** изменения перед коммитом: `git status`
2. **Не коммитьте** файлы с секретами (`.env`, токены)
3. **Используйте понятные** сообщения коммитов
4. **Делайте pull** перед push, если работаете с нескольких устройств
5. **Автоматические коммиты** создаются с префиксом `Auto-update:`

---

## 🔗 Полезные ссылки

- Репозиторий: https://github.com/consultingantifraud-hub/Media_lab
- GitHub CLI: https://cli.github.com/
- Git документация: https://git-scm.com/doc

