# 🔄 Автоматическое обновление на GitHub

## 📋 Обзор

Настроена система автоматического коммита изменений с сервера на GitHub с умной фильтрацией.

## 🎯 Практики обновления

### ✅ Что отправляется автоматически:

- **Код приложения** (`.py` файлы)
- **Конфигурация** (`.yml`, `.yaml`, `docker-compose.yml`)
- **Документация** (`.md`, `.txt`)
- **Docker файлы** (Dockerfile, .dockerignore)
- **Настройки Git** (.gitignore)

### ❌ Что НЕ отправляется:

- **Временные файлы** (`.tmp`, `.cache`)
- **Логи** (`.log`)
- **Кэш Python** (`__pycache__/`, `*.pyc`)
- **Секреты** (`.env` - уже в .gitignore)
- **Базы данных** (`*.db` - уже в .gitignore)
- **Медиа файлы** (`media/images/*`, `media/videos/*` - уже в .gitignore)

### ⏰ Частота обновлений

Рекомендуемые варианты:

1. **Каждый час** - для активной разработки
2. **Каждые 6 часов** - для стабильного проекта
3. **Раз в день** - для production окружения
4. **Вручную** - полный контроль

## 🚀 Установка автоматического обновления

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
cd /opt/media-lab
bash setup_auto_commit.sh
```

Скрипт предложит выбрать частоту обновлений и настроит cron.

### Вариант 2: Ручная настройка cron

```bash
# Редактировать cron
crontab -e

# Добавить строку (например, каждый час):
0 * * * * /opt/media-lab/auto_commit.sh >> /opt/media-lab/logs/git_cron.log 2>&1
```

## 📝 Как это работает

1. **Периодическая проверка** (через cron)
   - Скрипт проверяет изменения в репозитории
   - Фильтрует только важные файлы
   - Игнорирует временные и служебные файлы

2. **Умная фильтрация**
   - Добавляются только изменения в коде
   - Логи и временные файлы исключаются
   - Проверяется минимальное количество изменений

3. **Автоматический коммит**
   - Создается коммит с временной меткой
   - Отправляется на GitHub
   - Логируется в файл

## 🔧 Управление

### Ручной запуск

```bash
cd /opt/media-lab
bash auto_commit.sh
```

### Просмотр логов

```bash
# Логи автоматических коммитов
tail -f /opt/media-lab/logs/git_auto_commit.log

# Логи cron
tail -f /opt/media-lab/logs/git_cron.log
```

### Просмотр расписания

```bash
crontab -l | grep auto_commit
```

### Изменение частоты

```bash
bash setup_auto_commit.sh
# Выбрать новую частоту
```

### Отключение автоматического обновления

```bash
# Удалить задачу из cron
crontab -e
# Удалить строку с auto_commit.sh
```

## 📊 Примеры использования

### Сценарий 1: Активная разработка

```bash
# Установить обновление каждый час
bash setup_auto_commit.sh  # Выбрать вариант 1

# Каждый час все изменения кода будут отправляться на GitHub
```

### Сценарий 2: Production окружение

```bash
# Установить обновление раз в день
bash setup_auto_commit.sh  # Выбрать вариант 3

# Раз в день (в 02:00) все изменения отправляются на GitHub
```

### Сценарий 3: Полный контроль

```bash
# Не устанавливать cron, запускать вручную
bash setup_auto_commit.sh  # Выбрать вариант 5

# Запускать когда нужно:
bash auto_commit.sh
```

## ⚙️ Настройка фильтрации

В файле `auto_commit.sh` можно настроить:

- `MIN_CHANGES` - минимальное количество изменений для коммита
- Список игнорируемых расширений
- Шаблон сообщения коммита

## 🔍 Мониторинг

### Проверка последнего коммита

```bash
cd /opt/media-lab
git log -1 --oneline
```

### Проверка синхронизации

```bash
cd /opt/media-lab
git fetch origin
git status
```

### Просмотр истории изменений

```bash
cd /opt/media-lab
git log --oneline -10
```

## ⚠️ Важные замечания

1. **Автоматические коммиты** создаются с префиксом `Auto-update:`
2. **Фильтрация** защищает от коммита временных файлов
3. **Логи** сохраняются для отладки
4. **Ручные коммиты** всегда имеют приоритет

## 🆚 Сравнение: Авто vs Ручной коммит

| Характеристика | Автоматический | Ручной |
|---------------|----------------|--------|
| Частота | По расписанию | По требованию |
| Фильтрация | Автоматическая | Ручная |
| Контроль | Ограниченный | Полный |
| Удобство | Высокое | Среднее |
| Риск ошибок | Низкий (фильтрация) | Зависит от внимательности |

## 📚 Дополнительно

- Подробная документация: `GIT_WORKFLOW.md`
- Скрипт обновления: `update_github.sh` (ручной)
- Автоматический скрипт: `auto_commit.sh`
- Настройка: `setup_auto_commit.sh`

