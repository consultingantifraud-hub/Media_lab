# Анализ проекта для развертывания на VPS reg.ru

## 📊 Обзор проекта

**Название:** Telegram Media Generator Service  
**Версия:** 1.0 (стабильная)  
**Стек технологий:**
- Python 3.12
- aiogram 3 (Telegram Bot Framework)
- RQ + Redis (асинхронная обработка задач)
- FastAPI (REST API)
- Docker & Docker Compose
- Fal.ai API (генерация и обработка изображений)

## 🏗️ Архитектура

### Компоненты системы

1. **Telegram Bot** (`app/bot/`)
   - Обработка команд и сообщений от пользователей
   - Управление диалогами и состояниями
   - Постановка задач в очередь Redis

2. **RQ Workers** (`app/workers/`)
   - Обработка задач генерации и редактирования изображений
   - Взаимодействие с Fal.ai API
   - Управление статусами задач

3. **REST API** (`app/web/`)
   - Health check endpoints
   - Проверка статуса задач
   - Мониторинг системы

4. **Redis** 
   - Очередь задач (RQ)
   - Хранение статусов задач
   - Кэширование (опционально)

5. **Fal.ai Provider** (`app/providers/fal/`)
   - Клиент для взаимодействия с Fal.ai API
   - Обработка изображений (сжатие, кодирование)
   - Управление моделями

### Поток данных

```
Пользователь → Telegram Bot → Redis Queue → RQ Worker → Fal.ai API → Результат → Пользователь
```

## 📦 Зависимости

### Основные библиотеки
- `aiogram==3.4.1` - Telegram Bot Framework
- `fastapi==0.110.0` - REST API
- `uvicorn[standard]==0.29.0` - ASGI сервер
- `httpx==0.27.0` - HTTP клиент
- `redis==5.0.1` - Redis клиент
- `rq==1.15.1` - Redis Queue
- `loguru==0.7.2` - Логирование
- `Pillow==10.3.0` - Обработка изображений
- `deep-translator==1.11.4` - Перевод промптов

### Системные требования
- Python 3.12+
- Redis 7+
- Docker & Docker Compose
- Минимум 2 ГБ RAM (рекомендуется 4 ГБ+)
- Минимум 20 ГБ свободного места на диске

## 🔧 Конфигурация

### Переменные окружения

**Обязательные:**
- `tg_bot_token` - Токен Telegram бота
- `fal_api_key` - API ключ Fal.ai
- `app_env` - Режим работы (`local` или `vps`)

**Опциональные:**
- `redis_url` - URL Redis (по умолчанию `redis://localhost:6379/0`)
- `api_host` - Хост API (по умолчанию `0.0.0.0`)
- `api_port` - Порт API (по умолчанию `8000`)
- `log_level` - Уровень логирования (по умолчанию `INFO`)

### Модели Fal.ai

Проект использует следующие модели:
- `fal-ai/nano-banana` - Премиум модель для создания изображений
- `fal-ai/flux-pro/v1.1-ultra` - Стандартная модель
- `fal-ai/bytedance/seedream/v4/edit` - Редактирование и объединение
- `fal-ai/chrono-edit` - Редактирование изображений
- `fal-ai/recraft/upscale/crisp` - Улучшение качества
- `fal-ai/retoucher` - Ретушь
- `fal-ai/face-swap` - Замена лица

## 🚀 Развертывание

### Варианты развертывания

1. **Docker Compose** (рекомендуется)
   - Простое управление
   - Изоляция сервисов
   - Автоматический перезапуск
   - Используется `docker-compose.prod.yml`

2. **Systemd сервисы**
   - Автозапуск при загрузке системы
   - Интеграция с системным мониторингом
   - Файлы в `deploy/systemd/`

3. **Ручной запуск**
   - Для разработки и тестирования
   - Требует установки зависимостей вручную

### Структура развертывания

```
/opt/media-lab/
├── app/                    # Исходный код приложения
├── media/                  # Медиа файлы (изображения, видео)
│   ├── images/
│   ├── edits/
│   ├── face_swap/
│   └── videos/
├── deploy/                 # Файлы развертывания
│   ├── docker-compose.prod.yml
│   ├── scripts/
│   ├── systemd/
│   └── ...
├── .env                    # Конфигурация (не в git)
└── requirements.txt        # Python зависимости
```

## 🔒 Безопасность

### Рекомендации

1. **Переменные окружения**
   - Никогда не коммитить `.env` файл в git
   - Использовать сильные пароли и токены
   - Регулярно обновлять API ключи

2. **Firewall**
   - Закрыть все порты кроме необходимых
   - Открыть только: 22 (SSH), 80/443 (HTTP/HTTPS, если используется)
   - Redis должен быть доступен только локально

3. **Docker**
   - Использовать официальные образы
   - Регулярно обновлять образы
   - Не запускать контейнеры от root (где возможно)

4. **Мониторинг**
   - Настроить логирование
   - Мониторить использование ресурсов
   - Настроить алерты при ошибках

## 📈 Масштабирование

### Текущие ограничения

- **Fal.ai API:** 10 concurrent tasks на один API ключ
- **Redis:** Один экземпляр (можно настроить репликацию)
- **Workers:** Можно запустить несколько воркеров для параллельной обработки

### Рекомендации для масштабирования

1. **Увеличение воркеров:**
   ```yaml
   # В docker-compose.prod.yml можно добавить:
   worker-image-2:
     # ... копия worker-image с другим именем
   ```

2. **Redis кластер:**
   - Для высокой нагрузки настроить Redis Sentinel или Cluster

3. **Балансировка нагрузки:**
   - Использовать несколько API ключей Fal.ai
   - Распределять запросы между ключами

4. **Мониторинг:**
   - Настроить Prometheus + Grafana
   - Отслеживать метрики производительности

## 🐛 Известные особенности

1. **Автоматическое сжатие изображений**
   - Изображения больше 4 МБ автоматически сжимаются
   - Используется JPEG с качеством 50-85
   - Может потребоваться дополнительное сжатие для очень больших файлов

2. **Перевод промптов**
   - Промпты автоматически переводятся на английский
   - Исключение: Nano-banana поддерживает русский язык

3. **Очередь задач**
   - Задачи обрабатываются асинхронно
   - При превышении лимитов Fal.ai задачи ставятся в очередь
   - Статус задач можно проверить через API

## 📝 Логирование

### Уровни логирования

- **INFO** - Стандартные операции (рекомендуется для production)
- **DEBUG** - Детальная информация (только для разработки)

### Расположение логов

- Docker: `docker-compose logs`
- Systemd: `journalctl -u media-lab-*.service`
- Файлы: `/opt/media-lab/logs/` (если настроено)

## 🔄 Обновление

### Процесс обновления

1. Получить последние изменения (git pull)
2. Обновить зависимости (`pip install -r requirements.txt`)
3. Пересобрать Docker образы (`docker-compose build`)
4. Перезапустить сервисы (`docker-compose restart`)

Или использовать скрипт: `./scripts/update.sh`

## 📊 Мониторинг

### Метрики для отслеживания

1. **Производительность:**
   - Время обработки задач
   - Количество задач в очереди
   - Использование CPU и памяти

2. **Надежность:**
   - Количество ошибок
   - Успешность обработки задач
   - Доступность сервисов

3. **Ресурсы:**
   - Использование дискового пространства
   - Использование памяти
   - Сетевая активность

### Инструменты

- `docker stats` - Использование ресурсов контейнерами
- `./monitoring/healthcheck.sh` - Проверка здоровья сервисов
- `./monitoring/cleanup.sh` - Очистка старых данных

## ✅ Чеклист готовности к production

- [ ] Все переменные окружения заполнены
- [ ] Firewall настроен
- [ ] Автозапуск настроен (systemd)
- [ ] Резервное копирование настроено
- [ ] Мониторинг настроен
- [ ] Логирование настроено
- [ ] Тестирование выполнено
- [ ] Документация обновлена


