# Telegram Media Generator Service

Телеграм‑бот для генерации и обработки изображений через Fal Model APIs.  
Стек: **Python 3.12**, **aiogram 3**, **RQ + Redis**, **FastAPI**, **Docker Compose**.

**Версия:** 1.0 (стабильная)

## ✨ Ключевые особенности

- ✅ **Поддержка русского языка** для моделей Nano Banana Pro и Nano-banana (создание и объединение изображений)
- ✅ **Генерация промптов через LLM** — кнопка "Написать" создает детальные промпты без противоречий
- ✅ **Автоматическое сжатие** больших изображений для соответствия лимитам API
- ✅ **Асинхронная обработка** всех операций через queue API
- ✅ **8 режимов работы**: создание, написание промпта, редактирование, объединение, ретушь, добавление текста, замена лица, улучшение качества
- ✅ **Топовые модели**: Nano Banana Pro, Nano-banana, Seedream, Chrono Edit и другие

---

## 🚀 Основные возможности

Бот предоставляет полный набор инструментов для работы с изображениями:

### 🎨 **Создать** — Генерация изображений из текста

**Модели (в порядке приоритета):**
1. **Nano Banana Pro** (`fal-ai/gpt-image-1-mini/edit`) — лучшее качество кириллицы
   - ✅ **Поддерживает русский язык** — промпт можно писать на русском без перевода
   - Максимальное качество для детализированных изображений
   - Параметры: 50 шагов, guidance_scale 8.0
2. **Nano-banana** (`fal-ai/nano-banana`) — топовая модель от Google
   - ✅ **Поддерживает русский язык** — промпт можно писать на русском без перевода
   - Высокое качество для детализированных изображений
   - Параметры: 50 шагов, guidance_scale 8.0
3. **Seedream (Create)** (`fal-ai/bytedance/seedream/v4/text-to-image`) — топовая модель ByteDance
   - Отлично работает с текстом на изображениях
   - Промпт автоматически переводится на английский язык

**Процесс:**
1. Напишите промпт (например: "маленькая собака в шляпе, студийный свет")
2. Нажмите "🎨 Создать"
3. Выберите модель: "Nano Banana Pro", "Nano-banana" или "Seedream (Create)"
4. Выберите формат: Вертикальное / Квадрат / Горизонтальное
5. Получите готовое изображение

**Форматы:**
- **Вертикальное** (портрет): 1472×2048px
- **Квадрат**: 1792×1792px
- **Горизонтальное** (ландшафт): 2048×1472px

**Расширенный формат:**
```
Изображение <промпт> | model=fal-ai/flux/dev | size=1024x1024 | aspect_ratio=1:1 | negative=...
```

---

### ✍️ **Написать** — Генерация промпта для создания изображения

**Модель:** `anthropic/claude-3.5-sonnet` через `fal-ai/any-llm`

**Процесс:**
1. Нажмите "✍️ Написать"
2. Опишите желаемое изображение простыми словами (например: "красивая девушка на пляже")
3. Получите детальный промпт без противоречий и двойных трактовок
4. Используйте сгенерированный промпт в кнопке "🎨 Создать"

**Особенности:**
- ✅ **Избегает противоречий** — промпт проверяется на логическую согласованность
- ✅ **Структурированный промпт** — описание по приоритетам (объект → внешность → окружение → освещение → стиль)
- ✅ **Детальное описание** — минимум 1000 символов для максимальной детализации
- ✅ **Подходит для Edit моделей** — промпт может использоваться с референсными изображениями
- ✅ **Без технических параметров** — только описание изображения

**Пример:**
- Вход: "красивая девушка на пляже"
- Выход: детальный промпт с описанием внешности, одежды, окружения, освещения и стиля

---

**Примечание:** Кнопка "✏️ Редактировать" доступна под результатами генерации для быстрого редактирования только что созданного изображения. Для полноценного редактирования используйте кнопку "✏️ Изменить" в главном меню.

---

### ✏️ **Изменить** — Умное редактирование изображений

Универсальный инструмент для изменения изображений: добавление объектов, объединение нескольких изображений, редактирование сцены, добавление текста.

**Модели:**
- **Nano Banana Pro** (`fal-ai/nano-banana-pro/edit`) — лучшее качество кириллицы, цена выше в 3 раза
  - ✅ **Поддерживает русский язык** — промпт можно писать на русском без перевода
  - Отлично работает с текстом на изображениях
- **Nano-Banana** (`fal-ai/nano-banana/edit`) — качественное изменение объектов и сцен
  - ✅ **Поддерживает русский язык** — промпт можно писать на русском без перевода
  - Лучше работает с одним объектом и детальным промптом
  - Для нескольких объектов рекомендуется использовать Seedream
- **Seedream** (`fal-ai/bytedance/seedream/v4/edit`) — лучше работает с людьми, добавляет объекты
  - Промпт автоматически переводится на английский язык
  - Лучше работает с несколькими объектами и людьми

**Процесс:**
1. Нажмите "✏️ Изменить"
2. Выберите модель
3. Отправьте до 4 изображений (как фото или документ) или одно изображение для редактирования
4. Опишите желаемые изменения:
   - Для **Nano Banana Pro** и **Nano-Banana**: можно писать на русском языке
   - Для **Seedream**: рекомендуется английский язык (автоматический перевод)
   - Примеры: "добавь девушку справа", "объедини 3 изображения в одну сцену", "удали фон", "измени цвет неба на красный"
5. 💡 **Совет**: Можно добавить текст в изображение — укажите название или текст в описании, и модель добавит его в результат
6. Получите измененное изображение

**Особенности:**
- Работает с 1-4 изображениями
- Поддержка добавления объектов, людей, текста
- Поддержка объединения нескольких изображений в одну сцену
- Поддержка редактирования (удаление объектов, изменение цвета и т.д.)
- Результат в формате PNG, размер 1024×1024px по умолчанию (для Nano Banana Pro можно выбрать формат)
- Автоматическое сжатие больших изображений (более 4 МБ) для соответствия лимитам API

---

### ✨ **Ретушь** — Деликатная ретушь лица

Профессиональная ретушь портретов с сохранением естественности.

**Режимы:**
- **Мягкая ретушь** (`fal-ai/retoucher`)
  - Удаляет мелкие дефекты (прыщи, пятна)
  - Выравнивает тон кожи
  - Сохраняет естественную текстуру и поры
  - Не изменяет структуру лица
  
- **Усилить черты** (`fal-ai/bytedance/seedream/v4/edit`)
  - Улучшает четкость черт лица и кожи
  - Слегка улучшает яркость глаз
  - Сохраняет структуру лица, пропорции и возраст
  - **Не изменяет губы** — оставляет их без изменений
  - **Не омолаживает** — сохраняет исходный возраст
  - **Не добавляет новые объекты** — только улучшает существующие черты

**Процесс:**
1. Нажмите "✨ Ретушь"
2. Отправьте фото лица (как фото или документ, до 10 МБ)
3. Выберите режим: "Мягкая ретушь" или "Усилить черты"
4. (Опционально) Укажите дополнительную инструкцию:
   - Примеры для "Мягкая ретушь": "убери прыщи на лбу", "сделай кожу более гладкой", "убери темные круги под глазами"
   - Примеры для "Усилить черты": "улучши четкость черт", "сделай кожу более гладкой"
5. Или нажмите "Пропустить" для автоматической ретуши
6. Получите отретушированное изображение

**Ограничения:**
- Максимальный размер файла: 10 МБ
- Модели не изменяют структуру лица — только улучшают качество и убирают дефекты
- Режим "Усилить черты" не изменяет возраст, губы и не добавляет новые объекты

---

### 📝 **Добавить текст** — Stylish Text

Добавление надписей и эмодзи на изображения с помощью Pillow (локальная обработка).

**Технология:** Pillow (PIL) + Pilmoji для рендеринга текста и эмодзи

**Процесс:**
1. Нажмите "📝 Добавить текст"
2. Отправьте изображение (PNG/JPEG)
3. Введите текст, который нужно добавить (можно использовать многострочный текст и эмодзи)
4. Опишите пожелания по оформлению текста (или отправьте `/skip` для настроек по умолчанию)
5. Получите изображение с добавленным текстом

**Примеры пожеланий по оформлению:**
- "Крупный белый текст в центре, на чёрной плашке"
- "Огромный текст по центру, красный цвет, без плашки"
- "Обычный текст снизу, белая плашка с прозрачностью 50%"
- "Текст размером 72px снизу, расстояние от низа 10%"
- "Белая плашка с размытием, радиус размытия 5"

**Размеры текста:**
- Маленький / Средний / Обычный / Крупный / Огромный
- Или цифрами: 48px, 72 пикселей, размер 96

**Особенности:**
- ✅ **Локальная обработка** — не требует API, работает быстро
- ✅ **Поддержка русского языка** — полная поддержка кириллицы
- ✅ **Поддержка эмодзи** — рендеринг эмодзи через локальные Twemoji PNG файлы
- ✅ **Гибкие настройки** — размер, цвет, позиция, плашка, прозрачность, размытие
- ✅ **Автоматический выбор цвета** — контрастный цвет текста на основе фона
- ✅ **Умный перенос текста** — автоматический перенос длинных строк
- ✅ **LLM-парсинг пожеланий** — понимает естественный язык для настройки оформления

---

### 🔄 **Заменить лицо** — Face Swap

Замена лица на фотографии с сохранением естественности.

**Модель:** **WaveSpeed Face Swap** (`wavespeed-ai/image-face-swap`) — высокое качество через WaveSpeedAI
- ✅ Лучшее качество и сходство с исходным лицом
- ✅ Поддержка PNG формата для максимального качества
- ✅ Более точная замена лица
- ✅ Требует настройки `WAVESPEED_API_KEY` в `.env`

**Процесс:**
1. Нажмите "🔄 Заменить лицо"
2. Отправьте портрет с лицом (источник)
3. Отправьте фото, в котором нужно заменить лицо
4. Получите результат с замененным лицом

**Особенности:**
- ✅ Асинхронная обработка через queue API
- ✅ Автоматическое определение лиц
- ✅ Сохранение освещения и ракурса
- ✅ WaveSpeed Face Swap обеспечивает лучшее качество и сходство

---

### ⬆️ **Улучшить** — Upscale

Улучшение качества изображений с увеличением разрешения.

**Модель:** `fal-ai/recraft/upscale/crisp` (×2 масштаб)

**Процесс:**
1. Нажмите "⬆️ Улучшить"
2. Отправьте изображение (как фото или документ)
3. Получите улучшенное изображение в формате PNG

**Особенности:**
- Увеличение разрешения в 2 раза
- Автоматическая конвертация WebP в PNG
- Асинхронная обработка через queue API
- Результат отправляется по URL (Telegram скачивает файл со своей стороны)
- Поддержка больших файлов (до 7-8 МБ)

**Доступность:**
- Кнопка доступна в главном меню
- Кнопка "🔍 Upscale" появляется под результатами генерации и редактирования
- Можно улучшить любое загруженное изображение

---

## 📋 Команды

- `/start` — запуск бота, очистка состояния, инструкция
- `/menu` — очистка состояния и возврат в главное меню
- `/status <job_id>` — проверка статуса задачи
- `/ping` — проверка работоспособности бота
- `/add_balance <amount>` — пополнение баланса (только для администраторов)
  - Пример: `/add_balance 500` — пополнит баланс на 500₽
- `ℹ️ Info` — сброс текущего режима и возврат в главное меню

## 📋 Главное меню

При запуске бота (`/start`) доступны следующие кнопки:

- **🎨 Создать** — генерация изображений (Nano Banana Pro, Nano-banana, Seedream)
- **✍️ Написать** — генерация промпта для создания изображения
- **✏️ Изменить** — умное редактирование изображений (добавление объектов, объединение, изменение)
- **✨ Ретушь** — деликатная ретушь лица
- **📝 Добавить текст** — добавление текста на изображение
- **🔄 Заменить лицо** — замена лица на фотографии
- **⬆️ Улучшить** — улучшение качества изображения (Upscale)

---

## 🔧 Технические детали

### Асинхронная обработка

Все длительные операции выполняются асинхронно через **queue API** с polling:
- Генерация изображений
- Изменение изображений (Smart Merge/Edit)
- Ретушь
- Face Swap (базовая модель и WaveSpeed Face Swap)
- Upscale

Это обеспечивает надежную обработку без блокировки бота и возможность обработки больших файлов.

### Форматы и размеры файлов

- **Генерация**: PNG формат, автоматическая оптимизация размера
- **Редактирование**: PNG формат
  - Автоматическое сжатие изображений больше 4 МБ для соответствия лимитам API (10 МБ после base64)
  - Сжатие применяется только при необходимости, сохраняя качество маленьких файлов
- **Изменить**: PNG формат, 1024×1024px по умолчанию (для Nano Banana Pro можно выбрать формат)
  - Автоматическое сжатие больших изображений перед отправкой
- **Ретушь**: PNG формат, максимальный входной файл 10 МБ
- **Upscale**: PNG формат, увеличение в 2 раза

### Автоматическое сжатие изображений

Бот автоматически сжимает изображения перед отправкой в API, если их размер превышает лимиты:
- **Порог сжатия**: 4 МБ (до base64 кодирования)
- **Лимит API**: 10 МБ после base64 кодирования
- **Метод**: JPEG сжатие с постепенным снижением качества (85→50) и, при необходимости, уменьшение размера изображения
- **Результат**: Изображения меньше 4 МБ не сжимаются, сохраняя максимальное качество

### Сохранение результатов

- Все результаты сохраняются локально в `media/`:
  - `media/images/` — сгенерированные изображения
  - `media/edits/` — отредактированные изображения, ретушь, face swap, upscale
- Бот автоматически отправляет результат пользователю
- Файлы доступны для последующего использования (кнопка "Редактировать" под результатом)

---

## 🛠️ Подготовка окружения

### 1. Конфигурация `.env`

Заполните файл `.env` (если нет – скопируйте `env` или `env.example`):
```env
tg_bot_token=YOUR_TELEGRAM_BOT_TOKEN
app_env=local
api_host=0.0.0.0
api_port=8000
redis_url=redis://localhost:6379/0
fal_api_key=YOUR_FAL_KEY
fal_queue_base_url=https://queue.fal.run
fal_standard_model=fal-ai/flux/dev
fal_premium_model=fal-ai/nano-banana
fal_inpaint_model=fal-ai/flux-general/inpainting
fal_edit_model=fal-ai/chrono-edit
fal_upscale_model=fal-ai/recraft/upscale/crisp
fal_retoucher_model=fal-ai/retoucher
fal_face_enhance_model=fal-ai/bytedance/seedream/v4/edit
fal_face_swap_model=fal-ai/face-swap
wavespeed_api_key=YOUR_WAVESPEED_API_KEY  # Обязательно для WaveSpeed Face Swap
wavespeed_face_swap_model=wavespeed-ai/image-face-swap  # Модель WaveSpeedAI для замены лица
video_poll_interval=5
video_max_wait_sec=900
media_dir=./media
DATABASE_URL=sqlite:////tmp/media_lab_shared.db  # Общий файл БД для всех контейнеров
```

**Важно для production:**
- `WAVESPEED_API_KEY` — обязателен для работы Face Swap (используется только WaveSpeed модель)
- `DATABASE_URL` — должен указывать на общий файл для синхронизации между контейнерами

### 2. Redis и воркеры (Docker)

```powershell
cd docker
docker compose build worker-image
docker compose up -d redis worker-image
```

Контейнер `docker-worker-image-1` слушает очередь `img_queue`, Redis доступен на `localhost:6379`.

### 3. Бот локально

```powershell
cd C:\MAIN\Bots\Media_lab  # путь к проекту
python -m app.bot.main
```

Бот будет работать через long polling. Логи и ошибки появляются в этом же окне.

Опционально можно запустить `docker compose up -d bot` (нужно поправить команду в compose на `python -m app.bot.main`).

### 4. Проверка

- Отправьте `/start` или `/menu` — бот сбросит состояние и покажет инструкцию
- Протестируйте любую функцию из главного меню
- Проверьте логи воркера: `docker compose logs -f worker-image`

---

## 💰 Система биллинга и баланса

### Бизнес-логика

**Начальный баланс:**
- При регистрации каждому пользователю начисляется **30₽** на баланс
- Логика бесплатных генераций удалена — все операции оплачиваются с баланса

**Списание средств:**
- Средства списываются только после успешного завершения операции
- Операция создается со статусом `PENDING` при резервировании
- После успешного выполнения операция переводится в статус `CHARGED` и списывается баланс
- При ошибке операция переводится в статус `FAILED` без списания средств

**История операций:**
- Все операции сохраняются в базе данных
- В истории отображается время в московском часовом поясе (Мск)
- Доступна через кнопку "💰 Баланс" → "📊 История операций"

**Пополнение баланса:**
- Команда `/add_balance <amount>` для администраторов
- Пример: `/add_balance 500` — пополнит баланс на 500₽

### База данных (SQL)

**Архитектура:**
- Используется **SQLite** с общим файлом для всех контейнеров
- Файл базы данных: `/tmp/media_lab_shared.db`
- Синхронизация через **bind mounts** в Docker Compose

**Конфигурация:**
```yaml
# docker-compose.prod.yml
volumes:
  - /tmp/media_lab_shared.db:/tmp/media_lab_shared.db:rw

environment:
  - DATABASE_URL=sqlite:////tmp/media_lab_shared.db
```

**Синхронизация между контейнерами:**
- Бот и воркеры используют один и тот же файл базы данных
- После каждого изменения (`reserve_operation`, `confirm_operation`) база данных синхронизируется через `shutil.copy2`
- Это обеспечивает консистентность данных между контейнерами с `network_mode: host`

**Структура таблиц:**
- `users` — пользователи (telegram_id, has_free_access)
- `balances` — балансы пользователей (user_id, balance)
- `operations` — операции (user_id, type, price, status, created_at, task_id)
  - Статусы: `PENDING`, `CHARGED`, `FREE`, `FAILED`

**Важно:**
- Для production используйте абсолютный путь к файлу БД: `/tmp/media_lab_shared.db`
- Bind mount должен быть настроен для всех сервисов (bot, worker-image, worker-image-2, worker-image-3)
- Переменная окружения `DATABASE_URL` должна быть установлена во всех контейнерах

---

## 📊 Логи и диагностика

- **Воркера**: `docker compose logs -f worker-image`
- **Redis**: `docker compose logs -f redis`
- **Бот**: логи в консоли (если запущен локально) или `docker compose logs -f bot`
- **Статус задачи**: `/status <job_id>` в Telegram
- Для 400/500 от API в логах выводится тело ответа (например, «модель временно недоступна»)

---

## 🧹 Очистка Docker

Docker может занимать много места на диске из-за старых образов, build cache и остановленных контейнеров.

### Быстрая очистка

#### Безопасный режим (рекомендуется, по умолчанию):
```powershell
cd docker
.\cleanup-docker.ps1
```

Или через BAT файл:
```cmd
cd docker
cleanup-docker.bat
```

Скрипт безопасно удалит:
- Неиспользуемые образы (dangling и неиспользуемые)
- Build cache
- Остановленные контейнеры
- Неиспользуемые сети
- **Volumes НЕ удаляются** (безопасный режим по умолчанию)
- **Папка `media/` НЕ очищается** (по умолчанию)

**Важно:**
- **Работающие контейнеры и их образы будут сохранены** - можно запускать даже когда Docker и бот работают!
- **Bind mounts (`../media:/app/media`) НЕ удаляются** - это папки на хосте
- Данные в папке `media/` остаются нетронутыми (по умолчанию)
- Удаляются ТОЛЬКО остановленные контейнеры и неиспользуемые образы

#### Очистка с удалением данных в `media/`:

Если вы хотите также удалить содержимое папки `media/` (изображения, видео и т.д.):

```powershell
cd docker
.\cleanup-docker.ps1 -CleanMedia
```

Или используйте специальный скрипт:
```cmd
cd docker
cleanup-docker-with-media.bat
```

**⚠ ВНИМАНИЕ:** Это удалит все файлы в папке `media/` (images, videos, edits, face_swap и т.д.). Действие необратимо!

### Автоматическая очистка

Настройте автоматическую очистку через Планировщик задач Windows (рекомендуется раз в неделю).  
Подробная инструкция: [`docker/DOCKER_CLEANUP_README.md`](docker/DOCKER_CLEANUP_README.md)

### Проверка размера

Проверить текущее использование дискового пространства:
```powershell
docker system df
```

---

## 📁 Структура проекта

```
app/
  bot/                # aiogram-бот, обработчики, клавиатуры
    handlers/         # обработчики команд и кнопок
    keyboards/        # клавиатуры для пользователей
    services/         # сервисы для работы с очередями
  core/               # конфигурация, логирование, очереди
  providers/fal/      # клиенты и мапперы fal.ai
  web/                # FastAPI-приставка
  workers/            # RQ-воркеры (image/video)
docker/               # Dockerfile'ы и compose
media/                # результаты генераций
  images/            # сгенерированные изображения
  edits/             # отредактированные изображения, ретушь, face swap, upscale
```

---

## ❓ FAQ

### Почему нет изображения?
Проверьте логи воркера — возможно, выбранная модель временно отключена (например, ответ «Temporarily disabled…»). Попробуйте другую кнопку или `model=...`.

### Как задать свою модель?
Отправьте `Изображение <промпт> | model=имя`. Алиасы (`dalle3`, `flash`, `flux`) поддерживаются.

### Как изменить размер?
Через клавиатуру выбирайте вертикальный, квадратный или горизонтальный формат. Для тонкой настройки всё ещё можно передать `| size=1024x1024` вручную.

### Как изменить изображение?
Нажмите `✏️ Изменить`, выберите модель, загрузите изображение (или до 4 изображений) и опишите желаемые изменения. Модели поддерживают добавление объектов, объединение изображений, удаление элементов и изменение сцены. После получения результата, при необходимости, нажмите `⬆️ Улучшить`, чтобы увеличить изображение.

### Как работает ретушь?
Выберите режим: **Мягкая ретушь** для удаления дефектов и выравнивания тона кожи, или **Усилить черты** для улучшения четкости черт лица. Можно указать дополнительную инструкцию (например, "убери прыщи на лбу") или пропустить для автоматической ретуши. Модели не изменяют структуру лица — только улучшают качество.

### Как работает кнопка "Изменить"?
Отправьте до 4 изображений (или одно для редактирования), выберите модель, опишите желаемые изменения. Модель может объединить изображения, добавить объекты, удалить элементы или изменить сцену. 
- **Nano Banana Pro** и **Nano-Banana**: можно писать промпт на русском языке
- **Seedream**: промпт автоматически переводится на английский язык
Можно добавить текст, указав его в описании изменений.

### Как работает кнопка "Написать"?
Кнопка "✍️ Написать" использует LLM (Claude 3.5 Sonnet) для генерации детальных промптов без противоречий. Просто опишите желаемое изображение простыми словами, и получите структурированный промпт, готовый для использования в кнопке "🎨 Создать". Промпт проверяется на логическую согласованность и избегает двойных трактовок.

### Где итоговые файлы?
По умолчанию в `media/images` и `media/edits` (монты через docker compose). Бот также отправляет документ в Telegram.

### Почему Upscale возвращает WebP вместо PNG?
Модель может возвращать WebP, но бот автоматически конвертирует его в PNG перед отправкой.

### Почему файл после Upscale маленький (185 КБ)?
Если скачивание не удалось, файл отправляется по URL. Telegram может сжать его. Бот пытается скачать файл локально для гарантии качества, но если это не удается, отправляет по URL.

### Поддерживает ли бот русский язык?
Да! Модели **Nano Banana Pro** и **Nano-banana** полностью поддерживают русский язык:
- При создании изображений через "🎨 Создать" → "Nano Banana Pro" или "Nano-banana"
- При объединении изображений через "🔗 Объединить" → "Nano-Banana (Merge)"
- При генерации промптов через "✍️ Написать" — промпты создаются на русском языке
Промпт можно писать на русском языке без перевода. Для других моделей (Seedream) промпт автоматически переводится на английский язык.

### Что делать, если изображение слишком большое?
Бот автоматически сжимает изображения больше 4 МБ перед отправкой в API. Сжатие применяется только при необходимости и не влияет на качество маленьких файлов. Если вы видите сообщение об ошибке размера файла, попробуйте уменьшить разрешение изображения перед загрузкой.

---

## 📝 Планы

- Видеогенерация (подключение real API к `worker-video`)
- История задач (`/my`, пагинация)
- Хранение результатов в облаке (S3/MinIO) и выдача ссылок
- Вебхук-режим и деплой на VPS
- Улучшение обработки ошибок и уведомлений пользователей

---

## 👥 Многопользовательский режим

### ✅ Возможности

Бот **поддерживает многопользовательский режим** и может использоваться несколькими пользователями одновременно.

**Лимиты fal.ai API:**
- **10 concurrent tasks** на один API ключ (по всем endpoint'ам)
- Лимит применяется ко всем пользователям бота, использующим один API ключ
- Для enterprise клиентов можно увеличить лимиты (связаться с fal.ai)

**Рекомендации для многопользовательского использования:**

1. **Один API ключ для всех пользователей:**
   - Все пользователи бота используют один общий API ключ
   - Лимит в 10 concurrent tasks распределяется между всеми пользователями
   - При превышении лимита запросы автоматически ставятся в очередь

2. **Мониторинг использования:**
   - Используйте Platform APIs fal.ai для отслеживания использования
   - Настройте уведомления при достижении лимита concurrent requests
   - Отслеживайте использование через dashboard: `https://fal.ai/dashboard`

3. **Для больших нагрузок:**
   - Свяжитесь с fal.ai для увеличения лимитов (enterprise план)
   - Рассмотрите возможность использования нескольких API ключей с балансировкой нагрузки
   - Используйте queue API для надежной обработки запросов (уже реализовано в боте)

**Текущая архитектура бота:**
- ✅ Асинхронная обработка через queue API — запросы не блокируются
- ✅ Автоматическая очередь при превышении лимитов
- ✅ Один API ключ может обслуживать множество пользователей
- ✅ Все операции выполняются через воркеры (RQ), что позволяет обрабатывать запросы параллельно

**Ограничения:**
- Максимум 10 одновременных задач на один API ключ
- При большом количестве пользователей запросы могут ставиться в очередь
- Для production с высокой нагрузкой рекомендуется enterprise план fal.ai

---

## 🚀 Развертывание на VPS сервере

Для развертывания проекта на VPS сервере (например, reg.ru) используйте файлы из папки `deploy/`.

### Быстрый старт

1. **Подготовка сервера:**
   ```bash
   cd /opt/media-lab/deploy
   chmod +x scripts/*.sh
   ./scripts/setup.sh
   ```

2. **Конфигурация:**
   ```bash
   cp deploy/env.prod.example .env
   nano .env  # Заполните tg_bot_token и fal_api_key
   ```

3. **Запуск:**
   ```bash
   ./scripts/start.sh
   ```

### Документация по развертыванию

- **`deploy/README.md`** - Полная документация по развертыванию
- **`deploy/INSTALL.md`** - Пошаговая инструкция по установке
- **`deploy/QUICK_START.md`** - Быстрый старт
- **`deploy/DEPLOYMENT_CHECKLIST.md`** - Чеклист развертывания
- **`deploy/STRUCTURE.md`** - Описание структуры папки deploy

### Структура папки deploy

```
deploy/
├── README.md                    # Полная документация
├── INSTALL.md                   # Инструкция по установке
├── QUICK_START.md              # Быстрый старт
├── env.prod.example            # Пример конфигурации
├── docker-compose.prod.yml     # Production Docker Compose
├── scripts/                    # Скрипты управления
├── systemd/                    # Systemd сервисы
├── nginx/                      # Конфигурация Nginx
└── monitoring/                 # Скрипты мониторинга
```

### Управление сервисами

```bash
cd /opt/media-lab/deploy

./scripts/start.sh      # Запуск
./scripts/stop.sh       # Остановка
./scripts/restart.sh    # Перезапуск
./scripts/status.sh     # Статус
./scripts/logs.sh       # Логи
./scripts/update.sh     # Обновление
./scripts/backup.sh     # Резервная копия
```

---

## 📝 История изменений

### 2025-11-25: Обновление системы биллинга и базы данных

**Изменения:**
- ✅ **Обновлена бизнес-логика баланса**
  - При регистрации каждому пользователю начисляется **30₽** на баланс
  - Удалена логика бесплатных генераций — все операции оплачиваются с баланса
  - Добавлена команда `/add_balance <amount>` для пополнения баланса администраторами
- ✅ **Реорганизована база данных для многоконтейнерной архитектуры**
  - Используется общий файл базы данных `/tmp/media_lab_shared.db` для всех контейнеров
  - Добавлены bind mounts в Docker Compose для синхронизации БД между контейнерами
  - Реализована автоматическая синхронизация БД после операций `reserve_operation` и `confirm_operation`
  - Установлена переменная окружения `DATABASE_URL=sqlite:////tmp/media_lab_shared.db` для всех сервисов
- ✅ **Улучшена система биллинга**
  - Все операции (генерация, редактирование, ретушь, upscale, face swap) корректно списывают баланс
  - Операции создаются со статусом `PENDING` и переводятся в `CHARGED` после успешного завершения
  - При ошибке операция переводится в статус `FAILED` без списания средств
  - История операций отображает время в московском часовом поясе (Мск)
- ✅ **Обновлен Face Swap**
  - Удалена базовая модель `fal-ai/face-swap`
  - Используется только **WaveSpeed Face Swap** (`wavespeed-ai/image-face-swap`) для высокого качества
  - Упрощен процесс: выбор модели удален, сразу запрашиваются изображения
  - Улучшено качество: используется PNG формат вместо JPEG
- ✅ **Обновлена ретушь "Усилить черты"**
  - Модель: `fal-ai/bytedance/seedream/v4/edit` (Seedream Edit)
  - Обновлен промпт: улучшение кожи и глаз без омоложения
  - **Губы не изменяются** — остаются без изменений
  - **Не добавляются новые объекты** — только улучшение существующих черт
  - **Не изменяется возраст** — сохраняется исходный возраст

### 2025-11-23: Обновление функционала генерации промптов и моделей

**Изменения:**
- ✅ **Улучшена кнопка "Написать"** — генерация промптов через LLM с проверкой на противоречия
  - Добавлена структурированная структура промпта (объект → внешность → окружение → освещение → стиль)
  - Промпт проверяется на логическую согласованность перед отправкой
  - Избегаются двойные трактовки и противоречия
  - Увеличена длина промпта до 1000+ символов для максимальной детализации
- ✅ **Обновлены параметры качества для Nano-banana**
  - `num_inference_steps`: увеличено с 36 до 50 для лучшей детализации
  - `guidance_scale`: увеличено с 7.0 до 8.0 для лучшего соответствия промпту
- ✅ **Обновлен порядок моделей в меню "Создать"**
  - Порядок: Nano Banana Pro → Nano-banana → Seedream (Create)
  - Обновлены комментарии в коде с правильным порядком приоритетности
- ✅ **Удалены устаревшие функции**
  - Удалена кнопка "Flux Ultra" и связанные процессы
  - Удалена кнопка "Дизайнерский текст" и связанные процессы
  - Обновлено название кнопки "Объединить" (без "➕ Добавить")

### 2025-11-20: Обновление Face Swap

**Изменения:**
- ✅ **Добавлена поддержка WaveSpeedAI** для замены лица с высоким качеством
  - Новая модель: `wavespeed-ai/image-face-swap` (WaveSpeed Face Swap)
  - Лучшее качество и сходство с исходным лицом по сравнению с базовой моделью
  - Поддержка PNG формата для максимального качества
- ✅ **Улучшен пользовательский опыт**
  - Добавлена заглушка при попытке отправить изображение без выбора модели
  - Упрощен процесс: все модели запускаются сразу без запроса текстовых инструкций
  - Улучшены сообщения для пользователя
- ✅ **Очистка кода**
  - Удалены устаревшие упоминания Replicate и easel-ai
  - Обновлены проверки моделей для актуальных WaveSpeedAI моделей
  - Улучшена читаемость кода

**Конфигурация:**
- Добавлены настройки `WAVESPEED_API_KEY` и `WAVESPEED_FACE_SWAP_MODEL` в `.env`
- `WAVESPEED_API_KEY` теперь обязателен для работы Face Swap

---

## 🔧 Технические детали базы данных

### SQLite в многоконтейнерной среде

**Проблема:**
При использовании нескольких контейнеров (bot, worker-image, worker-image-2, worker-image-3) с `network_mode: host` и относительными путями к SQLite, каждый контейнер создавал свой файл базы данных, что приводило к рассинхронизации данных.

**Решение:**
1. **Общий файл базы данных:**
   - Используется абсолютный путь: `/tmp/media_lab_shared.db`
   - Файл монтируется через bind mount во все контейнеры

2. **Синхронизация после операций:**
   ```python
   # После reserve_operation и confirm_operation
   import shutil
   db_path = db.bind.url.database.replace("sqlite:///", "")
   shared_db_path = "/tmp/media_lab_shared.db"
   if db_path != shared_db_path and os.path.exists(db_path):
       shutil.copy2(db_path, shared_db_path)
   ```

3. **Конфигурация Docker Compose:**
   ```yaml
   volumes:
     - /tmp/media_lab_shared.db:/tmp/media_lab_shared.db:rw
   
   environment:
     - DATABASE_URL=sqlite:////tmp/media_lab_shared.db
   ```

**Результат:**
- Все контейнеры используют один и тот же файл базы данных
- Изменения видны сразу во всех контейнерах
- Нет рассинхронизации данных между bot и worker'ами

---

## 📄 Лицензия

Проект для внутреннего использования. Убедитесь, что использование fal.ai и WaveSpeedAI соответствует условиям сервиса.

**Коммерческое использование:**
- Большинство моделей fal.ai доступны для коммерческого использования
- Проверьте метку "Commercial use" на странице каждой модели
- Некоторые модели доступны только для исследований (метка "Research only")
- WaveSpeedAI: проверьте условия использования на https://wavespeed.ai
