# Инструкция по первому подключению SSH

## 🔐 Первое подключение (одноразово)

При первом подключении к серверу SSH запросит подтверждение добавления ключа хоста в список доверенных.

### Шаг 1: Подключитесь к серверу

Выполните в PowerShell или терминале:

```powershell
ssh reg-ru-neurostudio
```

Или используйте скрипт:

```powershell
.\scripts\ssh-connect.ps1
```

### Шаг 2: Подтвердите добавление ключа

Когда появится запрос:
```
The authenticity of host '91.197.97.68 (91.197.97.68)' can't be established.
ED25519 key fingerprint is SHA256:CTg9oA8tu1xfisHsGbdbUPEzBB8o1g5Yxiv4WgyT+4U.
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

**Введите:** `yes` и нажмите Enter

### Шаг 3: Введите пароль (если требуется)

Если SSH ключ еще не настроен, введите пароль пользователя `root`.

### Шаг 4: Готово!

После первого успешного подключения все последующие подключения будут работать автоматически без подтверждений.

## ✅ Проверка подключения

После успешного подключения вы должны увидеть приглашение командной строки сервера:

```bash
root@your-server:~#
```

## 🔑 Настройка SSH ключей (рекомендуется)

Чтобы не вводить пароль каждый раз, настройте SSH ключи:

### 1. Проверьте, есть ли у вас SSH ключ:

```powershell
ls ~/.ssh/id_rsa.pub
```

### 2. Если ключа нет, создайте его:

```powershell
ssh-keygen -t rsa -b 4096
```

Нажмите Enter для всех вопросов (или укажите свой email).

### 3. Скопируйте публичный ключ на сервер:

```powershell
type ~/.ssh/id_rsa.pub | ssh reg-ru-neurostudio "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

Или используйте:

```powershell
ssh-copy-id reg-ru-neurostudio
```

### 4. Проверьте подключение без пароля:

```powershell
ssh reg-ru-neurostudio
```

Теперь подключение должно происходить без запроса пароля!

## 📝 Быстрые команды

```powershell
# Подключиться к серверу
ssh reg-ru-neurostudio

# Выполнить команду на сервере без входа
ssh reg-ru-neurostudio "docker ps"

# Скопировать файл на сервер
scp file.txt reg-ru-neurostudio:/path/to/destination/

# Скопировать файл с сервера
scp reg-ru-neurostudio:/path/to/file.txt ./
```







