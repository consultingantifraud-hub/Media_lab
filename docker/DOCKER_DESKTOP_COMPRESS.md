# Сжатие диска Docker Desktop

## ❌ Встроенной функции сжатия НЕТ

**Docker Desktop не имеет встроенной функции для сжатия диска** в графическом интерфейсе. Это нужно делать через системные инструменты Windows.

## 📁 Где находится диск Docker

Docker Desktop хранит данные в виртуальном диске (VHDX файл):
- Путь: `C:\Users\<Ваше_имя>\AppData\Local\Docker\wsl\disk\docker_data.vhdx`
- Или: `C:\Users\<Ваше_имя>\AppData\Local\Docker\wsl\data\ext4.vhdx` (в некоторых версиях)

## ✅ Решение: Используйте наши скрипты

Мы уже создали скрипты для сжатия диска Docker:

### Способ 1: Через скрипт (рекомендуется)

1. Найдите файл: `C:\MAIN\Bots\Media_lab\docker\compress-vhdx-admin.bat`
2. **Правой кнопкой** → **"Запуск от имени администратора"**
3. Дождитесь завершения

### Способ 2: Через PowerShell (если есть Hyper-V)

Если у вас установлен модуль Hyper-V:

```powershell
# Запустите PowerShell от имени администратора
wsl --shutdown
Optimize-VHD -Path "$env:LOCALAPPDATA\Docker\wsl\disk\docker_data.vhdx" -Mode Full
```

### Способ 3: Через Disk Management

1. Откройте `diskmgmt.msc`
2. **Действие** → **Присоединить виртуальный жесткий диск**
3. Выберите файл `docker_data.vhdx`
4. Правой кнопкой по диску → **Сжать виртуальный жесткий диск**
5. Отсоедините диск после сжатия

## ⚠️ Важно

- **Остановите Docker Desktop** перед сжатием
- **Остановите WSL**: `wsl --shutdown`
- Сжатие может занять **5-15 минут** для больших файлов
- Данные внутри Docker уже удалены, файл просто нужно сжать

## 📊 Текущая ситуация

- Данные внутри Docker: **~1.7 GB** (уже очищено)
- Размер файла VHDX: **29.65 GB** (нужно сжать)
- После сжатия должно быть: **~2-3 GB**

---

**Вывод:** Docker Desktop не имеет встроенной функции сжатия. Используйте наши скрипты или системные инструменты Windows.







