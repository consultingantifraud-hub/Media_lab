# ✅ Развертывание завершено!

## Что было сделано:

1. ✅ **SSH подключение настроено** - теперь можно подключаться без пароля
2. ✅ **Docker и Docker Compose установлены** на сервере
3. ✅ **Файлы проекта загружены** в `/opt/media-lab`
4. ✅ **Директории созданы** для медиа файлов
5. ✅ **Файл .env создан** из примера
6. ✅ **Права на скрипты установлены**

## 📝 Следующие шаги:

### 1. Настройте переменные окружения

Подключитесь к серверу и отредактируйте файл `.env`:

```bash
ssh reg-ru-neurostudio
cd /opt/media-lab
nano .env
```

Заполните обязательные переменные:
- `tg_bot_token` - токен вашего Telegram бота (получить у @BotFather)
- `fal_api_key` - API ключ fal.ai (получить на https://fal.ai/dashboard/keys)
- `app_env=vps` - режим работы (должен быть "vps")

Для сохранения в nano: `Ctrl+O` (сохранить), `Enter` (подтвердить), `Ctrl+X` (выйти)

### 2. Запустите сервисы

```bash
cd /opt/media-lab/deploy
./scripts/start.sh
```

### 3. Проверьте статус

```bash
./scripts/status.sh
```

### 4. Просмотрите логи

```bash
./scripts/logs.sh
```

Или для конкретного сервиса:
```bash
./scripts/logs.sh bot
./scripts/logs.sh api
./scripts/logs.sh worker-image
```

## 🔧 Полезные команды:

### Подключение к серверу:
```bash
ssh reg-ru-neurostudio
```

### Управление сервисами:
```bash
cd /opt/media-lab/deploy

# Запуск
./scripts/start.sh

# Остановка
./scripts/stop.sh

# Перезапуск
./scripts/restart.sh

# Статус
./scripts/status.sh
```

### Просмотр логов Docker:
```bash
docker-compose -f docker-compose.prod.yml logs -f
```

### Проверка контейнеров:
```bash
docker ps
docker-compose -f docker-compose.prod.yml ps
```

## ✅ Проверка работы:

1. Проверьте статус контейнеров: `docker ps`
2. Проверьте логи бота: `./scripts/logs.sh bot`
3. Протестируйте бота в Telegram - отправьте команду `/start`
4. Проверьте API: `curl http://localhost:8000/health`

## 🎉 Готово!

После заполнения переменных в `.env` и запуска сервисов ваш проект будет полностью развернут и готов к работе!







