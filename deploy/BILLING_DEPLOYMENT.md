# Инструкция по развертыванию системы биллинга

## 📋 Обзор

Данная инструкция описывает процесс внедрения системы оплаты и биллинга в Telegram Media Generator Bot.

## 🔧 Предварительные требования

1. **База данных:**
   - PostgreSQL (рекомендуется для production) или SQLite (для разработки)
   - Для PostgreSQL: создать базу данных `media_lab`

2. **Аккаунт ЮKassa:**
   - Зарегистрироваться на https://yookassa.ru
   - Получить `shop_id` и `secret_key`
   - Настроить webhook URL в личном кабинете

## 📦 Установка зависимостей

### 1. Обновить requirements.txt

Добавьте в `requirements.txt`:

```txt
# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# PostgreSQL driver (если используете PostgreSQL)
psycopg2-binary>=2.9.9
```

Или используйте файл `requirements-billing.txt`:

```bash
cat requirements-billing.txt >> requirements.txt
```

### 2. Установить зависимости

```bash
pip install -r requirements.txt
```

## ⚙️ Конфигурация

### 1. Обновить .env файл

Добавьте следующие переменные в `.env`:

```bash
# Billing Configuration
PRICE_PER_OPERATION=10
FREE_OPERATIONS_COUNT=4

# Database Configuration
# Для PostgreSQL:
DATABASE_URL=postgresql://user:password@localhost/media_lab
# Для SQLite (разработка):
# DATABASE_URL=sqlite:///./media_lab.db

# YooKassa Configuration
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
YOOKASSA_RETURN_URL=https://t.me/your_bot
YOOKASSA_CURRENCY=RUB
YOOKASSA_WEBHOOK_URL=https://your-domain.com/yookassa/webhook
```

### 2. Настройка PostgreSQL (если используется)

```bash
# Создать базу данных
sudo -u postgres psql
CREATE DATABASE media_lab;
CREATE USER media_lab_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE media_lab TO media_lab_user;
\q
```

Обновите `DATABASE_URL`:
```
DATABASE_URL=postgresql://media_lab_user:your_password@localhost/media_lab
```

## 🗄️ Применение миграций БД

### 1. Инициализация Alembic (если еще не сделано)

```bash
cd /opt/media-lab
alembic upgrade head
```

### 2. Применение миграций

```bash
# Применить все миграции
alembic upgrade head

# Откатить последнюю миграцию (если нужно)
alembic downgrade -1

# Просмотреть текущую версию
alembic current
```

## 🔗 Настройка webhook ЮKassa

### 1. URL webhook

Webhook endpoint доступен по адресу:
```
POST https://your-domain.com/yookassa/webhook
```

### 2. Настройка в личном кабинете ЮKassa

1. Войдите в личный кабинет ЮKassa
2. Перейдите в раздел "Настройки" → "HTTP-уведомления"
3. Укажите URL webhook: `https://your-domain.com/yookassa/webhook`
4. Сохраните настройки

### 3. Проверка доступности webhook

Убедитесь, что ваш сервер доступен из интернета по HTTPS:

```bash
# Проверить доступность API
curl https://your-domain.com/yookassa/health
```

## 🚀 Развертывание

### 1. Обновить код на сервере

```bash
cd /opt/media-lab
git pull  # или загрузить файлы другим способом
```

### 2. Применить миграции

```bash
alembic upgrade head
```

### 3. Обновить .env файл

```bash
nano .env
# Добавить переменные биллинга (см. выше)
```

### 4. Перезапустить сервисы

```bash
# Перезапустить API (для webhook)
sudo systemctl restart media-lab-api

# Перезапустить бота
sudo systemctl restart media-lab-bot

# Проверить статус
sudo systemctl status media-lab-api
sudo systemctl status media-lab-bot
```

### 5. Проверить логи

```bash
# Логи API (webhook)
sudo journalctl -u media-lab-api -f

# Логи бота
sudo journalctl -u media-lab-bot -f
```

## 📝 Интеграция в существующие обработчики

### Пример интеграции проверки баланса

В существующих обработчиках платных режимов добавьте декоратор:

```python
from app.bot.handlers.billing import check_balance_decorator

@router.message(...)
@check_balance_decorator("generate")  # или "edit", "merge", "face_swap", "retouch", "upscale"
async def handle_generate(message: Message, operation_id: int):
    # operation_id будет передан автоматически
    # Ваш существующий код обработки
    ...
```

### Платные режимы

Следующие режимы требуют оплаты (10₽ за операцию):
- 🎨 Создать (`generate`)
- ✏️ Редактировать (`edit`)
- 🔗 Объединить (`merge`)
- 🔄 Заменить лицо (`face_swap`)
- ✨ Ретушь (`retouch`)
- ⬆️ Улучшить (`upscale`)

### Бесплатный режим

- 📝 Добавить текст (`add_text`) - не требует оплаты и не тратит бесплатные операции

## 🔍 Проверка работы

### 1. Проверить команду /balance

Отправьте боту `/balance` - должно показать баланс и количество бесплатных операций.

### 2. Проверить создание платежа

1. Нажмите кнопку "💰 Оплатить / Баланс"
2. Выберите сумму
3. Должна появиться ссылка на оплату

### 3. Проверить webhook

После успешной оплаты в логах API должно появиться сообщение о обработке webhook.

## 🐛 Решение проблем

### Ошибка подключения к БД

```bash
# Проверить подключение PostgreSQL
psql -U media_lab_user -d media_lab -h localhost

# Проверить переменную DATABASE_URL
echo $DATABASE_URL
```

### Миграции не применяются

```bash
# Проверить текущую версию
alembic current

# Просмотреть историю миграций
alembic history

# Применить миграции принудительно
alembic upgrade head --sql  # показать SQL без выполнения
alembic upgrade head         # применить
```

### Webhook не работает

1. Проверить доступность URL из интернета
2. Проверить логи API: `sudo journalctl -u media-lab-api -f`
3. Проверить настройки в личном кабинете ЮKassa
4. Убедиться, что используется HTTPS

### Баланс не пополняется после оплаты

1. Проверить логи webhook в API
2. Проверить статус платежа в БД:
   ```sql
   SELECT * FROM payments WHERE status = 'succeeded' ORDER BY created_at DESC LIMIT 5;
   ```
3. Проверить баланс пользователя:
   ```sql
   SELECT u.telegram_id, b.balance FROM users u JOIN balances b ON u.id = b.user_id WHERE u.telegram_id = YOUR_TELEGRAM_ID;
   ```

## 📊 Структура БД

### Таблицы

- `users` - пользователи бота
- `balances` - балансы пользователей
- `payments` - платежи через ЮKassa
- `operations` - операции биллинга (списания/бесплатные)

### Полезные SQL запросы

```sql
-- Статистика по пользователям
SELECT COUNT(*) as total_users, SUM(b.balance) as total_balance 
FROM users u LEFT JOIN balances b ON u.id = b.user_id;

-- Статистика по платежам
SELECT status, COUNT(*) as count, SUM(amount) as total 
FROM payments 
GROUP BY status;

-- Статистика по операциям
SELECT type, status, COUNT(*) as count, SUM(price) as total 
FROM operations 
GROUP BY type, status;
```

## 📚 Расположение файлов

### Биллинг
- `app/db/` - модели БД и настройка
- `app/services/billing.py` - сервис биллинга
- `app/services/payment.py` - сервис платежей ЮKassa
- `app/bot/handlers/billing.py` - обработчики оплаты и баланса

### Миграции
- `alembic/` - конфигурация Alembic
- `alembic/versions/` - файлы миграций

### Webhook
- `app/web/billing.py` - webhook endpoint для ЮKassa

## 🔐 Безопасность

1. **Никогда не коммитьте `.env` файл** в git
2. **Используйте HTTPS** для webhook URL
3. **Ограничьте доступ** к БД только с локального сервера
4. **Регулярно проверяйте логи** на подозрительную активность
5. **Используйте сильные пароли** для БД и API ключей

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи сервисов
2. Статус БД
3. Настройки ЮKassa
4. Переменные окружения




